/**
 * Todavía no hay login (ver backend/src/routes/clienteRoutes.js, está vacío).
 * Hasta que exista un caso de uso de autenticación, simulamos que el cliente
 * que usa la app es siempre este id. Cuando se agregue login, este valor
 * pasa a venir del usuario autenticado en vez de estar hardcodeado.
 */
export const CLIENTE_ACTUAL_ID = 1;
export const GIG_ACTUAL_ID = 1;